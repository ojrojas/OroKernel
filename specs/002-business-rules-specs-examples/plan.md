# Implementation Plan: Business Rules & Specification Pattern Examples

**Branch**: `002-business-rules-specs-examples` | **Date**: 2026-06-06 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/002-business-rules-specs-examples/spec.md`

## Summary

Add concrete example implementations of `IBusinessRule` and `BaseSpecification<T>` to the existing `UserManagement` example project, demonstrating real validation scenarios (e.g., unique email, active user check) and filtering logic (e.g., active-only users). Include corresponding unit tests in `Shared.Tests`.

## Technical Context

**Language/Version**: C# 13 / .NET 10.0.102

**Primary Dependencies**: No new dependencies — uses existing `OroKernel.Shared` library (IBusinessRule, BaseSpecification, Error, Result)

**Storage**: In-memory collections for specification examples; EF Core InMemory for business rule examples that query the database

**Testing**: xUnit 2.9.3 + Moq 4.20.72 + Shouldly (existing in Shared.Tests)

**Target Platform**: .NET console application (examples) + class library (tests)

**Project Type**: Library with example console apps and test project

**Performance Goals**: N/A — example/learning code

**Constraints**: Must compile with 0 errors/warnings; must not break existing 64 tests; must not add new NuGet dependencies

**Scale/Scope**: 2-3 business rule classes, 2-3 specification classes, corresponding unit tests integrated into existing example projects

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### Constitution Gates

The following MUST be verified against `.specify/memory/constitution.md`:

1. **Security** - No hardcoded secrets; input validation for rule parameters; no sensitive data exposure in error messages
2. **Code Quality** - SOLID/DRY/KISS/YAGNI followed; rules are single-purpose; specifications follow the established pattern
3. **Architecture** - Layer separation preserved; business rules in examples/domain area; specifications alongside existing entities
4. **Testing** - Every rule + spec has pass/fail tests; coverage maintained at 84%+ overall; no existing tests broken
5. **Dependencies** - No new dependencies introduced; existing OroKernel.Shared interfaces reused
6. **Performance** - N/A (example code, no production hot paths)
7. **Observability** - N/A (example code, no logging requirements)
8. **Documentation** - README updated to mention new examples if they add a new scenario pattern

## Project Structure

### Documentation (this feature)

```text
specs/002-business-rules-specs-examples/
    plan.md              # This file
    research.md          # Phase 0 output
    data-model.md        # Phase 1 output
    quickstart.md        # Phase 1 output
    contracts/           # Phase 1 output (N/A — no external interfaces)
    tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
examples/
    UserManagement/
        User.cs                            # Existing
        Program.cs                         # Updated to demonstrate rules/specs
        Data/
            UserManagementDbContext.cs     # Existing
        BusinessRules/                     # NEW — business rule examples
            UserEmailMustBeUniqueRule.cs
            UserMustBeActiveRule.cs
            MinimumUserNameLengthRule.cs
        Specifications/                    # NEW — specification examples
            ActiveUserSpecification.cs
            UserByEmailSpecification.cs
            UserNameContainsSpecification.cs

src/
    Shared.Tests/
        Examples/                          # NEW — tests for example code
            UserManagement/
                BusinessRules/
                    UserEmailMustBeUniqueRuleTests.cs
                    UserMustBeActiveRuleTests.cs
                    MinimumUserNameLengthRuleTests.cs
                Specifications/
                    ActiveUserSpecificationTests.cs
                    UserByEmailSpecificationTests.cs
                    UserNameContainsSpecificationTests.cs
```

**Structure Decision**: Business rules and specifications live in the UserManagement example project (alongside existing `User.cs` and `Program.cs`). Tests mirror this structure under `Shared.Tests/Examples/UserManagement/`.

## Complexity Tracking

> No Constitution violations expected — this is additive example code with zero breaking changes.

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| N/A | N/A | N/A |
